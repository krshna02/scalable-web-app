# Full Starter Kit — Frontend (Next.js) + Backend (Express) with JWT Auth

This repository contains a ready-to-run starter kit implementing authentication (JWT), a dashboard,
CRUD on `tasks`, basic security (bcrypt + JWT), Postman collection, and scaling notes.

See frontend/ and backend/ folders for code. Follow each folder's README for setup.
